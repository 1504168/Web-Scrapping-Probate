#
#
#
# update_cookies()
# k = accept_continue()
# all_decedent_info = get_search_results(k, 30, 9, 2024)
# # dump the extracted info to a json file
#
# with open('Extracted Info/Ohio.json', 'w') as f:
#     json.dump(all_decedent_info, f, indent=4)
